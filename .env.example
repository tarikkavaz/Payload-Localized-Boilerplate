# =============================================================================
# ENVIRONMENT CONFIGURATION
# =============================================================================
# Set to 'development' for local development, 'production' for live deployment
NODE_ENV=development

# =============================================================================
# PAYLOAD CMS CONFIGURATION
# =============================================================================
# Used to encrypt JWT tokens and secure your application
# Generate a new secret with: openssl rand -hex 32
# IMPORTANT: Use a strong, unique secret in production
PAYLOAD_SECRET=YOUR_SECRET_HERE

# Path to your Payload configuration file (usually don't need to change this)
PAYLOAD_CONFIG_PATH=src/payload.config.ts

# Set to true to automatically seed the database with sample data on first run
# Set to false in production after initial setup
PAYLOAD_SEED=true

# =============================================================================
# SERVER URLs
# =============================================================================
# URL where your Payload admin panel will be accessible
# For local development: http://localhost:3000
# For production: https://yourdomain.com
PAYLOAD_PUBLIC_SERVER_URL=http://localhost:3000

# URL for your Next.js frontend application
# Should match PAYLOAD_PUBLIC_SERVER_URL in most cases
NEXT_PUBLIC_SERVER_URL=http://localhost:3000

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
# Database connection string
# For SQLite (development): file:./website.db
# For MongoDB: mongodb://localhost:27017/your-database-name
# For PostgreSQL: postgresql://user:password@localhost:5432/database
DATABASE_URI=file:./website.db

# =============================================================================
# DRAFT PREVIEW & REVALIDATION
# =============================================================================
# Secrets used to preview draft content before publishing
# Generate unique secrets for production: openssl rand -hex 32
PAYLOAD_PUBLIC_DRAFT_SECRET=demo-draft-secret
NEXT_PRIVATE_DRAFT_SECRET=demo-draft-secret

# Keys used to revalidate static pages when content changes
# Generate unique keys for production: openssl rand -hex 32
REVALIDATION_KEY=demo-revalation-key
NEXT_PRIVATE_REVALIDATION_KEY=demo-revalation-key

# =============================================================================
# EMAIL CONFIGURATION (Resend)
# =============================================================================
# Get your API key from: https://resend.com/api-keys
# Leave empty for development if you don't need email functionality
RESEND_API_KEY=

# Default sender email address for system emails
# Must be a verified domain in your Resend account for production
DEFAULT_FROM_ADDRESS=dev@localhost

# Default sender name for system emails
DEFAULT_FROM_NAME=Development

# =============================================================================
# SEO & INDEXING
# =============================================================================
# Set to 'true' to allow search engines to index your site
# Leave empty or set to 'false' for development/staging environments
NEXT_PUBLIC_IS_LIVE=

# =============================================================================
# THEME CONFIGURATION
# =============================================================================
# Controls if Hero components can override the header theme
# Set to 'true' to enable, 'false' to disable
NEXT_PUBLIC_HEADER_THEME_OVERRIDE_ENABLED=false

# =============================================================================
# DEVELOPMENT TOOLS
# =============================================================================
# Your preferred React editor for better development experience
REACT_EDITOR=cursor

# =============================================================================
# GOOGLE reCAPTCHA (Optional - for form protection)
# =============================================================================
# Get keys from: https://www.google.com/recaptcha/admin
# Uncomment and configure when you're ready to add reCAPTCHA protection
# NEXT_PUBLIC_GOOGLE_RECAPTCHA_SITE_KEY=your_site_key_here
# GOOGLE_RECAPTCHA_SECRET_KEY=your_secret_key_here
# GOOGLE_RECAPTCHA_VERSION=v3

# =============================================================================
# DEPLOYMENT CONFIGURATION (Optional - for automated deployment)
# =============================================================================
# SSH connection details for your production server
# Format: username@server-ip-address
# DEPLOY_SERVER=deployer@your-server-ip

# Path to your application directory on the production server
# DEPLOY_PATH=/path/to/your/app